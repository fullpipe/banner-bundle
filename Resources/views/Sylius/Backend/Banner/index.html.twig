{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'FullpipeBannerBundle:Sylius/Backend/Banner:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.banner.index'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="pull-right">
        {{ buttons.create(path('fullpipe_sylius_backend_banner_create'), 'sylius.banner.create'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-bullhorn"></i> {{ 'sylius.banner.index_header'|trans|raw }}</h1>
</div>

{{ pagination(banners) }}
{{ list(banners) }}
{{ pagination(banners) }}

{% endblock %}
